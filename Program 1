/******************************************************************************
Student name:Jaxon Talbert
Student NetID: Jrt524
Compiler Used: GDB online
Program Description:
The following program asks a user to enter strings until STOP is entered and then
reads the strings back
in reverse order before ending the program.
*******************************************************************************/
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define BUF_SIZE 1024
int main(int argc, char *argv[])
{
//initalize line for user input
char line[BUF_SIZE];
//initialize array
char arrayString[BUF_SIZE][BUF_SIZE];
//initialize i and j integers
int i = 0;
int j;
//Notify user that STOP will end program
printf("Enter STOP to end the program.\n");
//while not STOP
while(1)
{
//Inform user to enter string
printf("please enter a string: \n");
//Scan in string
scanf("%s", line);
//if STOP then break
if (strncmp(line, "STOP", 4) == 0) break;
//copy into array
strcpy(arrayString[i], line);
//add one to i to increment array
i = i + 1;
}
printf("The strings will now be displayed in reverse. \n");
//set i = j and decrement to read array backwards
for (j = i; j >= 0; j--)
{
//print array
printf("%s\n", arrayString[j]);
}
return 0;
}
